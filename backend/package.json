{
  "name": "fpl-tracker-backend",
  "version": "1.0.0",
  "description": "FPL Tracker backend — caches FPL API data and serves it to the Android app",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.3",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
